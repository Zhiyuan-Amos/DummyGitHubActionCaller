on: [workflow_dispatch]

jobs:
  dummy_job:
    runs-on: ubuntu-latest
    name: Dummy Job
    steps:
      - name: Dummy Step
        id: id
        uses: Zhiyuan-Amos/DummyGitHubAction@v1.0.1
        with:
          inline-variable: 'a'
          configuration-variable: 'b'
