on: [workflow_dispatch]

jobs:
  dummy_job:
    runs-on: self-hosted
    name: Dummy Job
    steps:
      - name: Dummy Step
        id: id
        uses: Zhiyuan-Amos/DummyGitHubAction@main
        with:
          inline-variable: 'a'
          configuration-variable: 'b'
